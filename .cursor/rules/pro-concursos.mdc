---
description: Regras específicas para desenvolvimento e manutenção do sistema PRO Concursos
alwaysApply: false
---
# PRO Concursos - Regras de Desenvolvimento

## 🎯 **Contexto do Sistema**

O PRO Concursos é uma plataforma de estudos para concursos públicos composta por:
- **pro-frontend**: Interface web principal (saladeestudos.html)
- **pro-backend**: Servidor backend
- **pro-questoes**: Sistema de questões (questoes.proconcursos.com.br)
- **PRO_Import_System**: Sistema local de importação de conteúdo
- **Firebase Storage**: Armazenamento de aulas, metadados e questões

## 📋 **Regras de Desenvolvimento**

### **1. Arquitetura e Estrutura**
- **SEMPRE** responder em português brasileiro
- **NUNCA** criar arquivos desnecessários - prefira editar existentes
- **SEMPRE** manter compatibilidade com sistema de isolamento CSS (Fases 1-5)
- **SEMPRE** preservar funcionalidades existentes ao fazer modificações
- **NUNCA** criar arquivos teste para o usuario - TODOS OS TESTES SAO PUBLICADOS POR PUSH NO GITHUB para teste real (IA pode testar e deletar arquivos de teste caso queira)

### **2. Sistema de Carregamento Dinâmico**
- **SEMPRE** usar Shadow DOM para isolamento de conteúdo
- **SEMPRE** implementar fallback para modo tradicional
- **SEMPRE** usar ResourceCache para otimização de performance
- **SEMPRE** sanitizar HTML antes da exibição
- **NUNCA** quebrar o fluxo: Cursos → Módulos → Aulas → Questões

### **3. Integração com Firebase**
- **SEMPRE** tratar URLs do Firebase Storage (gs://) adequadamente
- **SEMPRE** implementar fallback para URLs públicas
- **SEMPRE** manter cache de conteúdo para performance
- **SEMPRE** preservar metadados de cursos/módulos/aulas

### **4. CSS e Estilos**
- **SEMPRE** usar variáveis CSS do sistema (--main-color, --accent-1, etc.)
- **SEMPRE** manter compatibilidade com modo escuro
- **SEMPRE** usar core-styles.css como base
- **NUNCA** interferir com estilos dos arquivos de conteúdo isolados

### **5. JavaScript e Funcionalidades**
- **SEMPRE** manter compatibilidade com Mermaid.js
- **SEMPRE** preservar interatividade de tabelas
- **SEMPRE** manter sistema de notificações
- **SEMPRE** implementar tratamento de erros robusto

### **6. Performance e Otimização**
- **SEMPRE** usar cache inteligente (ResourceCache)
- **SEMPRE** implementar lazy loading quando apropriado
- **SEMPRE** otimizar carregamento de recursos externos
- **SEMPRE** manter logs informativos para debug

### **7. Acessibilidade e UX**
- **SEMPRE** manter navegação por teclado
- **SEMPRE** preservar indicadores visuais de carregamento
- **SEMPRE** manter responsividade
- **SEMPRE** implementar feedback visual para ações do usuário

## 🔧 **Padrões de Código**

### **Classes e Métodos**
```javascript
// SEMPRE usar comentários descritivos
class NomeDaClasse {
    constructor() {
        // Inicialização clara
    }
    
    /**
     * Método com documentação JSDoc
     * @param {string} param - Descrição do parâmetro
     * @returns {Promise<Object>} Descrição do retorno
     */
    async metodoExemplo(param) {
        try {
            // Implementação
        } catch (error) {
            console.error('❌ Erro específico:', error);
            throw error;
        }
    }
}
```

### **Tratamento de Erros**
```javascript
// SEMPRE implementar try/catch com logs informativos
try {
    // Operação
    console.log('✅ Operação realizada com sucesso');
} catch (error) {
    console.error('❌ Erro na operação:', error);
    // Fallback ou tratamento
}
```

### **Cache e Performance**
```javascript
// SEMPRE verificar cache antes de carregar
if (this.cache.has(key) && this.isCacheValid(cached)) {
    return this.cache.get(key);
}
```

## 📁 **Estrutura de Arquivos**

### **Arquivos Principais**
- `saladeestudos.html` - Aplicação principal (NÃO CRIAR NOVOS)
- `core-styles.css` - Estilos base do sistema
- `header_saladeestudos.html` - Cabeçalho dinâmico
- `styles.css` - Estilos específicos

### **Arquivos de Conteúdo**
- Localizados em `PRO_Import_System/content/`
- Estrutura: `{curso}/{modulo}/aula-XX.html`
- **NUNCA** modificar diretamente - usar sistema de importação

## 🚫 **Restrições Importantes**

### **NUNCA FAZER:**
- Criar novos arquivos HTML principais
- Modificar estrutura do Firebase sem backup
- Quebrar compatibilidade com Shadow DOM
- Remover funcionalidades de isolamento
- Alterar URLs do Firebase Storage
- Criar dependências externas desnecessárias

### **SEMPRE VERIFICAR:**
- Compatibilidade com navegadores modernos
- Funcionamento do sistema de isolamento
- Integridade do cache
- Preservação de funcionalidades existentes

## 🔍 **Debugging e Monitoramento**

### **Logs Padrão**
```javascript
console.log('✅ Sucesso:', mensagem);
console.warn('⚠️ Aviso:', mensagem);
console.error('❌ Erro:', mensagem);
console.log('🔄 Processando:', mensagem);
console.log('📋 Cache:', mensagem);
```

### **Métodos de Debug**
- `studyApp.getCacheStats()` - Estatísticas do cache
- `studyApp.performHealthCheck()` - Health check do sistema
- `studyApp.getIsolationInfo()` - Status do isolamento

## 📚 **Recursos de Referência**

### **Documentação Interna**
- `plano-reengenharia-saladeestudos.md` - Plano completo de reengenharia
- `documentacao-fase2-isolamento.md` - Documentação do isolamento
- `relatorio-fase1-auditoria.md` - Auditoria inicial

### **APIs Disponíveis**
- Firebase Auth, Firestore, Storage
- Sistema de isolamento CSS (Fases 1-5)
- ResourceCache e FallbackManager
- EventBridge para Shadow DOM

## 🎯 **Objetivos de Qualidade**

1. **Performance**: Carregamento rápido e cache eficiente
2. **Robustez**: Fallbacks e tratamento de erros
3. **Manutenibilidade**: Código limpo e documentado
4. **Compatibilidade**: Funcionamento em todos os navegadores
5. **Acessibilidade**: Interface acessível e responsiva

---

**Lembre-se**: O PRO Concursos é um sistema em produção usado por estudantes. Toda modificação deve ser feita com cuidado e sempre testada antes da implementação.